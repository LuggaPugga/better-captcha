name: Playwright Tests
on:
  push:
    branches:
      - main
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      matrix:
        project: [react, solidjs, vue, qwik, svelte, lit, preact] 
    steps:
      - uses: actions/checkout@v6

      - name: Set up Node
        uses: actions/setup-node@v6
        with:
          node-version: "26"

      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build Core
        run: bun --cwd packages/core run build

      - name: Build package
        run: bun --cwd packages/${{ matrix.project }} run build

      - name: Install Playwright Browsers
        run: bun --cwd packages/${{ matrix.project }}x playwright install

      - name: Run Playwright tests
        run: bun --cwd packages/${{ matrix.project }}x playwright test

      - uses: actions/upload-artifact@v7
        if: ${{ !cancelled() }}
        with:
          name: playwright-report-${{ matrix.project }}
          path: packages/${{ matrix.project }}/playwright-report/
          retention-days: 30